# Copy to .env.local. These are placeholders, not usable image names or secrets.
# Pin reviewed images already available in the local Docker Engine.
POSTGRES_IMAGE=REPLACE_WITH_POSTGRES_17_BOOKWORM_REPOSITORY_AT_SHA256
OBJECT_STORE_IMAGE=REPLACE_WITH_REVIEWED_SEAWEEDFS_REPOSITORY_AT_SHA256
APP_IMAGE=REPLACE_WITH_CERTIFIED_APP_REPOSITORY_AT_SHA256
GATEWAY_IMAGE=REPLACE_WITH_CERTIFIED_GATEWAY_REPOSITORY_AT_SHA256
WORKER_IMAGE=REPLACE_WITH_CERTIFIED_WORKER_REPOSITORY_AT_SHA256
# Match the host owner of .local-data and .local-secrets. Do not use root.
LOCAL_UID=REPLACE_WITH_ID_U
LOCAL_GID=REPLACE_WITH_ID_G
APP_PORT=8080
GATEWAY_PORT=8081
